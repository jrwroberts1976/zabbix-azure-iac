---
- name: Verify TimescaleDB role platform
  ansible.builtin.assert:
    that:
      - ansible_distribution == 'Debian'
      - ansible_distribution_major_version | int == 13
      - timescaledb_version is version('2.30.0', '<')
    fail_msg: "The TimescaleDB role currently targets Debian 13 with the approved Zabbix-compatible version."

- name: Install TimescaleDB repository prerequisites
  ansible.builtin.apt:
    name:
      - ca-certificates
      - curl
      - gnupg
      - python3-psycopg2
    state: present
    update_cache: true
    cache_valid_time: 3600

- name: Ensure APT keyring directory exists
  ansible.builtin.file:
    path: /etc/apt/keyrings
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: Install TimescaleDB repository signing key
  ansible.builtin.get_url:
    url: "{{ timescaledb_repository_key_url }}"
    dest: "{{ timescaledb_repository_key_path }}"
    owner: root
    group: root
    mode: "0644"

- name: Configure TimescaleDB APT repository
  ansible.builtin.copy:
    dest: /etc/apt/sources.list.d/timescale-timescaledb.sources
    owner: root
    group: root
    mode: "0644"
    content: |
      Types: deb
      URIs: {{ timescaledb_repository_uri }}
      Suites: {{ ansible_distribution_release }}
      Components: main
      Signed-By: {{ timescaledb_repository_key_path }}

- name: Pin approved TimescaleDB package pair
  ansible.builtin.copy:
    dest: /etc/apt/preferences.d/timescaledb-zabbix
    owner: root
    group: root
    mode: "0644"
    content: |
      Package: {{ timescaledb_package_name }} {{ timescaledb_loader_package_name }}
      Pin: version {{ timescaledb_package_version }}
      Pin-Priority: 1001

- name: Install pinned TimescaleDB packages
  ansible.builtin.apt:
    name:
      - "{{ timescaledb_package }}"
      - "{{ timescaledb_loader_package }}"
    state: present
    update_cache: true
    allow_downgrade: true
  register: timescaledb_package_install

- name: Read installed TimescaleDB package version
  ansible.builtin.command:
    argv:
      - dpkg-query
      - -W
      - "-f=${Version}"
      - "{{ timescaledb_package_name }}"
  register: timescaledb_installed_package
  changed_when: false

- name: Read installed TimescaleDB loader package version
  ansible.builtin.command:
    argv:
      - dpkg-query
      - -W
      - "-f=${Version}"
      - "{{ timescaledb_loader_package_name }}"
  register: timescaledb_installed_loader_package
  changed_when: false

- name: Require approved TimescaleDB package pair
  ansible.builtin.assert:
    that:
      - timescaledb_installed_package.stdout == timescaledb_package_version
      - timescaledb_installed_loader_package.stdout == timescaledb_package_version
    fail_msg: >-
      Installed TimescaleDB server and loader packages must both match
      the approved Zabbix-compatible version.

- name: Require approved TimescaleDB control version
  ansible.builtin.command:
    argv:
      - grep
      - -F
      - -x
      - "default_version = '{{ timescaledb_version }}'"
      - "/usr/share/postgresql/{{ postgresql_version }}/extension/timescaledb.control"
  changed_when: false

- name: Configure TimescaleDB preload library
  ansible.builtin.copy:
    dest: "/etc/postgresql/{{ postgresql_version }}/main/conf.d/98-timescaledb.conf"
    owner: postgres
    group: postgres
    mode: "0644"
    content: |
      # Managed by Ansible.
      shared_preload_libraries = 'timescaledb'
  register: timescaledb_preload

- name: Restart PostgreSQL after TimescaleDB package or preload change
  ansible.builtin.service:
    name: "{{ postgresql_service_name }}"
    state: restarted
    enabled: true
  when: >-
    timescaledb_package_install.changed or
    timescaledb_preload.changed

- name: Optionally run timescaledb-tune
  ansible.builtin.command:
    cmd: timescaledb-tune --quiet --yes
  when: timescaledb_run_tune | bool
  register: timescaledb_tune_result
  changed_when: timescaledb_run_tune | bool

- name: Restart PostgreSQL after timescaledb-tune
  ansible.builtin.service:
    name: "{{ postgresql_service_name }}"
    state: restarted
    enabled: true
  when:
    - timescaledb_run_tune | bool
    - timescaledb_tune_result is changed

- name: Ensure TimescaleDB extension exists in selected databases
  community.postgresql.postgresql_ext:
    name: timescaledb
    db: "{{ item }}"
    state: present
  become_user: postgres
  vars:
    ansible_become: true
    ansible_become_method: su
    ansible_become_flags: "-s /bin/sh"
  loop: "{{ timescaledb_databases }}"
